import {
  stringPluralize
} from 'hacker-news/helpers/string-pluralize';

module('StringPluralizeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = stringPluralize(42);
  ok(result);
});
