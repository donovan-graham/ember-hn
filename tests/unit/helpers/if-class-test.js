import { ifClass } from '../../../helpers/if-class';
import { module, test } from 'qunit';

module('Unit | Helper | if class');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ifClass(42);
  assert.ok(result);
});
