import {
  urlParse
} from 'hacker-news/helpers/url-parse';

module('UrlParseHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = urlParse(42);
  ok(result);
});
