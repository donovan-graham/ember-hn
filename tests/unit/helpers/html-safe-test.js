import {
  htmlSafe
} from 'hacker-news/helpers/html-safe';

module('HtmlSafeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = htmlSafe(42);
  ok(result);
});
