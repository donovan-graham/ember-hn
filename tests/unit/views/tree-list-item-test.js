import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:tree-list-item', 'TreeListItemView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
